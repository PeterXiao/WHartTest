[project]
name = "wharttest-actuator"
version = "0.1.0"
description = "UI自动化执行器服务"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "websockets>=12.0",
    "pydantic>=2.0",
    "httpx>=0.25.0",
]

[project.scripts]
actuator = "main:main"
